name: data
description: "Data layer for Cheq Gallery App"
version: 0.0.1

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  dartz: ^0.10.1
  domain:
    path: ../domain/
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  injectable: ^2.5.0
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  injectable_generator: ^2.6.2
  build_runner: ^2.4.14
